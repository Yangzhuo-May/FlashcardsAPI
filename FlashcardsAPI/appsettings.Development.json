{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "DefaultConnection": "Host=localhost;Port=5432;Database=FlashcardAPP;Username=postgres;Password=123456"
    },
    "Seeding": {
        "SeedDataOnStartup": true, // Set to true to enable seeding on startup
        "ClearExistingDataBeforeSeeding": false, // Set to true to clear all data before seeding
        "NumberOfUsersToSeed": 5, // Number of users to seed
        "StacksPerUser": 3, // Number of stacks per user
        "CardsPerStack": 5, // Number of cards per stack
        "AnswersPerCard": 4 // Number of answers per card (at least 1 correct)
    }
}
